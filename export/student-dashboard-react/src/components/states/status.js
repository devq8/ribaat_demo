/**
 * Every data-driven region takes the same `status` prop.
 * 'ready' still renders empty variants when its collection is empty.
 */
export const STATUS = {
  loading: 'loading',
  error: 'error',
  empty: 'empty',
  ready: 'ready',
};
